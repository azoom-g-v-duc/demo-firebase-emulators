# Requirements
- Firebase CLI version >= 7.8.0
# Init firebase
```cmd
  firebase init
  firebase init emulators
```
# Start emulators
```cmd
  firebase emulators:start
```
# Run tests
```cmd
  yarn
  yarn test
``` 